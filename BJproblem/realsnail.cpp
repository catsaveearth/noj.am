#include <stdio.h>

int main(void) {
	int snail[100][100] = { 0, };
	int n; //n*n짜리 수열임
	scanf("%d", &n);
	int j = 0, k = 0;
	for (int i = 1; i <= n * n; i++) {
		snail[j][k] = i;

		switch () {
		

		}

	}

}