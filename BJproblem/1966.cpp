#include <bits/stdc++.h>
using namespace std;
int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);
	int t, n, m, k;
	cin >> t;
	while (t--) {
		queue<int>q;
		cin >> n >> m;
		for (int i = 0; i < n; i++) {
			cin >> k;
			q.push(k);
		}

	}

}